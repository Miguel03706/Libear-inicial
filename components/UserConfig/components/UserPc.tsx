import React from "react";
import {
    Image, Center, Button, Input, useToast, Skeleton, Box, Grid, GridItem, VisuallyHidden, Text
} from "@chakra-ui/react";
import styles from "../UserConfig.module.scss";


function UserPc(){
    return(
        <>
       
        </>
    );
}

export default UserPc;